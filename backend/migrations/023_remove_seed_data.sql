-- +goose Up
-- This migration carried demo rows in an earlier, private version of the
-- project. The public copy ships without sample data, so it is intentionally
-- empty; the file stays so goose version numbers remain contiguous.

-- +goose Down
